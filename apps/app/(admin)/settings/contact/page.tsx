import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '문의 관리 — VisionFlow Admin',
  description: '문의 유형별 관리 허브',
};

export default function Contact() {
  return <div>Contact</div>;
}
