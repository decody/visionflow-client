{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @visionflow/admin build",
  "outputDirectory": "apps/admin/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
